CREATE TABLE USERS(

	ID INTEGER IDENTITY PRIMARY KEY,
	EMAIL NVARCHAR(255),
	PASSWORD VARCHAR(60),
	DOB DATE,
	PHONE_NO VARCHAR(22),
	AVATAR VARBINARY(MAX),
	DESCR VARCHAR(MAX),
	FOLLOWING_NO INT,
	FOLLOWERS_NO INT

)